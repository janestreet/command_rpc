(executables
 ((names (command_rpc_example))
  (libraries (
    async
    core
    command_rpc))))

(unified_tests
 ((target runtest)
  (deps (./command_rpc_example.exe))
  (setup_script setup-script)
  ))

(jbuild_version jane_street)
